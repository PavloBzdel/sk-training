import { InitNumberPipe } from './init-number.pipe';

describe('InitNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new InitNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
